
<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <form method="POST" action="/feed/{{problem._id}}?_method=PUT">
      {{> problem-form}}
      <!-- BUTTON -->
      <div class="form-group">
        <button class="btn btn-primary" type="submit">Save Problem</button>
        <button href="\">Cancel</button>
      </div>
    </form>
  </div>
</div>
